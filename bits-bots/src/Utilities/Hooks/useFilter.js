import React, { useState } from 'react';
import { useApi } from './useApi';
import { productUrl } from '../Constants/endpoints';

const useFilter = () => {
  const [filteredGames, setFilteredGames] = useState(null);

  return <div>Balaclava</div>;
};
